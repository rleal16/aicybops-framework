# AICybOps Service Configuration
# Copy this file to .env and adjust values for your deployment

# Service Configuration
AICYBOPS_SERVICE_HOST=localhost
AICYBOPS_SERVICE_PORT=8000
AICYBOPS_SERVICE_PROTOCOL=http

# Data Source Configuration (default: API — AICybOps fetches data from collect_metrics_api)
DATA_SOURCE=api  # 'api' = use RestAPI (DataCollector); 'local' = use local files
# URL of the monitoring host's collect_metrics_api (port 5010). Not InfluxDB (5007).
API_URL=http://<MONITORING_HOST_IP>:5010

# Model Configuration (for service)
SIMPLE_CNN_MODEL_NAME=simple_cnn
AUTOENCODER_MODEL_NAME=autoencoder

# PyTorch / DAM device (default deployment: CPU-only host)
# AICYBOPS_TORCH_VARIANT=cpu   # set in docker-compose; use "cuda" only on GPU images
# DAM_DEVICE=cpu                 # training/inference device; use cuda:0 when GPU is available

# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5001
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000

# MinIO Configuration
AWS_ACCESS_KEY_ID=minio
AWS_SECRET_ACCESS_KEY=minio123

# Database Configuration
DATABASE_URL=postgresql://mlflow:mlflow@localhost:5432/mlflow

# Service Internal Configuration
DATA_DIR=/app/data
LOCAL_DATA_SOURCE=/app/data/
CNN_AUTOENCODER_DATA_SOURCE=/app/data/main_test.csv
